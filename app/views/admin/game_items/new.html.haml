- content_for :page_title do
  새 게임 아이템
= simple_form_for @game_item, url: admin_game_items_path, html: { multipart: true } do |form|
  = render 'form', form: form
  = form.button :button, '등록', type: :submit
