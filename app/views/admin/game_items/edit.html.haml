- content_for :page_title do
  게임 아이템 수정
= simple_form_for @game_item, url: admin_game_item_path(@game_item), html: { multipart: true } do |form|
  = render 'form', form: form
  = form.button :button, '저장', type: :submit
