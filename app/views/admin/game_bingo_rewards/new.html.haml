- content_for :page_title do
  #{@event.title} · 보상 추가
= simple_form_for @reward, url: admin_game_bingo_event_rewards_path(@event) do |form|
  = render 'form', form: form
  = form.button :button, '추가', type: :submit
