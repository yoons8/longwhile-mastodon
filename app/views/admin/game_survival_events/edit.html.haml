- content_for :page_title do
  생존 이벤트 수정
= simple_form_for @event, url: admin_game_survival_event_path(@event), html: { multipart: true } do |form|
  = render 'form', form: form, game_items: @game_items
  = form.button :button, '저장', type: :submit
